# ISSUES — Aggregated (audit in progress: 96/432 files)

Flat, sortable list of every REAL issue found so far. Severity · file:line · description · fix.
Coverage to date: `src/types`, `src/constants`, `src/theme`, `src/schemas`, `src/lib`, `src/utils`, `src/services` (complete); `src/stores` (5/21). Everything below is verified against current code.

## CRITICAL
| ID | file:line | Issue | Fix |
|----|-----------|-------|-----|
| COMP-LOG-1 ✅FIXED | src/components/log/LogForm.tsx (status:138,rating:172,review:246,…) · src/hooks/useLogFlow.ts:409-418 · app/(modals)/log-modal.tsx:78 | **Film-logging form core fields are no-ops.** LogForm sent all field updates via `flow.dispatch({type:'SET_FIELD',field,value})`, but `useLogFlow.dispatch` only handled 6 premium fields (no default) → status/rating/review/date/etc. silently dropped. `validateLogSubmission` then blocked submit → **a user could not log a watched film.** Incomplete refactor (setters existed & worked; LogForm never migrated). | **FIXED** (commit 4f6df68) on `fix/log-form-core-fields`: migrated all 16 dispatch sites to typed setters, deleted 7 dead wrapper callbacks, **removed the dispatch shim** (reuse is now a compile error). Added LogForm.fields.test.tsx regression test. tsc/eslint/full-suite green. **Pending on-device check.** |

## HIGH
| ID | file:line | Issue | Fix |
|----|-----------|-------|-----|
| BACKEND-PROFILE-FREEZE-1 ⚠️VERIFY-LIVE | supabase/migrations/0002_rls_hardening.sql:14-35 | **`protect_profile_fields` BEFORE-UPDATE trigger unconditionally reverts role/tier/followers_count/following_count/total_logs on EVERY profile UPDATE** (no auth.role() guard; never superseded). BEFORE triggers fire for service-role + SECURITY DEFINER triggers too → tier upgrades (sync-entitlement) reverted → premium_rls (tier-gated) never passes → no premium for anyone; follower counts frozen at 0. (is_banned/founding/warning_count NOT protected, so moderation hides the bug.) CI never exercises triggers. | **Verify live trigger NOW.** Drop count columns from the trigger (let count-triggers manage them); gate role/tier revert on `auth.role()='authenticated'` so service-role sync works; make check_role_update the single role guard. Add trigger integration tests. |
| BACKEND-NOTIF-SPOOF-1 | supabase/migrations/0001_baseline_schema.sql:225 vs 0002_rls_hardening.sql:81 | **Notification spoofing — security fix defeated by a trailing-period typo.** Baseline's permissive INSERT policy `"Authenticated users can insert notifications"` (no `user_id` check) was meant to be dropped by 0002, but the DROP names it `"...notifications."` (extra period) → `IF EXISTS` silent no-op. No replacement policy. Any authed user can INSERT notifications to ANY user with arbitrary `type`/`from_username`/`message` → phishing/impersonation. | `DROP POLICY IF EXISTS "Authenticated users can insert notifications"` (correct name); rely on SECURITY DEFINER triggers (they bypass RLS). Add a cross-user-insert-denied test. |
| BACKEND-PAY-1 | supabase/functions/paytabs-handler/index.ts:7,92,125-129 | **PayTabs IPN webhook auth = URL shared-token with hardcoded default `'dev-secret-123'`.** If `PAYTABS_WEBHOOK_SECRET` unset in prod → anyone forges an IPN (`cart_id:"MEMBERSHIP\|uid\|founding"`, `response_status:'A'`) → free role/`is_founding` upgrades via service-role write. Even if set, secret-in-URL leaks via logs; no PayTabs HMAC `signature` verification. `/create` route IS JWT-gated (fine). | Remove the `'dev-secret-123'` fallback (fail closed); verify PayTabs HMAC signature over the raw body; add `tran_ref` idempotency to block replay. |
| TYPES-1 ✅FIXED | src/types/flash-list.d.ts:18 · app/lounge/[id].tsx:507 · src/stores/lounge.ts:302,327 | **`inverted` is a no-op in FlashList 2.0.2** (verified in installed pkg); lounge chat removed its manual `.reverse()` relying on it and has no fallback scroll positioning → chat opened at the oldest message; `onEndReached` fired at the wrong end vs `loadMoreMessages` (loads older→prepends top); author/time grouping used `index+1` (newer). NOTE: `keyboardShouldPersistTaps`/`keyboardDismissMode` are NOT no-ops (inherited from ScrollViewProps in v2) — only `inverted`/`estimatedItemSize` are dead. | **FIXED** on `fix/lounge-flashlist-v2-chat-ordering`: `maintainVisibleContentPosition:{startRenderingFromBottom,autoscrollToBottomThreshold}` + `onStartReached` + grouping `index-1`. tsc/eslint clean. Pending on-device check; `.d.ts` cleanup deferred. |

## MEDIUM
| ID | file:line | Issue | Fix |
|----|-----------|-------|-----|
| LIB-1 ✅FIXED | src/lib/revenueCat.ts:182-201 · app/(modals)/membership.tsx:105,163 | `purchaseTier` matched RC **package** identifier by substring (`<tier>_annual`/`<tier>`); RC package defaults are `$rc_annual` etc. Live purchases may always throw "No package found". | **FIXED** (commit 7e1753e): match on `pkg.product.identifier`/`packageType` via pure exported `selectPackageForTier`; collect across all offerings (topology-agnostic); legacy package-id match kept as final fallback. 7-test suite green. **Caveat: RC dashboard must still have the products configured.** |
| LIB-2 ✅FIXED | src/lib/revenueCat.ts:51-71 | RevenueCat init/config failures log only via `__DEV__` console → invisible in production (disables all monetization silently). | **FIXED** (commit 338ca14): init paths routed through `logger` (forwards to Sentry) — missing key → warn, failure → error (stack preserved), success → info. |
| OFFQ-1 ✅FIXED | src/utils/offlineQueue.ts:280-307 · src/utils/networkError.ts:13-29 | Transient 500/429/408 fall through to dead-letter (only 502/503/504 are "network") → silent permanent loss of queued offline writes. | **FIXED** (commit 6620dd0): added `isTransientError` + bounded per-mutation `_retryCount` (envelope) — transient failures halt-and-retry up to 5 flushes, then dead-letter. Unit + 4 integration tests; full suite green. |
| SVC-1 ✅VERIFIED-REAL (sharper) | src/utils/mutationExecutor.ts:333-339 | **Offline list-comment notification insert is broken → silent notification loss.** It inserts `{type:'list_comment', actor_id, reference_id, entity_id}` — but those 3 columns exist in NO migration, `'list_comment'` violates the notifications `type` CHECK constraint, and it omits the NOT NULL `message`. The insert is NOT `throwIfError`-wrapped → fails silently → an offline-queued list comment never notifies the list owner. (Online path via `StackService`/trigger uses the valid `'comment'` shape — works.) | Emit the notification from the DB trigger only (single source), OR fix the offline insert to the canonical `{user_id,type:'comment',from_username,message,...}` shape with valid columns/type. |
| NOTIF-1 ⬇️DOWNGRADED to LOW (was overstated) | src/stores/notificationStore.ts:103-107,161 | Original claim ("one invalid row blanks the screen") is **not realizable**: `notifications.message` is NOT NULL + a `type` CHECK constraint exists → malformed rows can't be persisted (the broken SVC-1 insert *fails* rather than storing a bad row); and on `safeParse` failure the store `return`s and keeps MMKV-cached data (`:106,119`) — never blank. Real residual = robustness nit: all-or-nothing `z.array().safeParse` silently no-ops the whole refresh if a row is ever malformed by future schema drift (vs per-row salvage used elsewhere). | LOW: switch to per-row salvage for resilience; not a live blank-screen bug. |
| BACKEND-PAY-2 | supabase/functions/paytabs-handler/index.ts:151-156 | **PayTabs founding path bypasses the atomic seat counter** — sets `is_founding=true` via direct `profiles.update`, not `claim_founding_seat` RPC → web founding purchases can over-sell the 100-seat cap. FOUND-1's fix only covers the RevenueCat/sync-entitlement path. | Route the PayTabs founding grant through `claim_founding_seat(userId)`; honor its boolean (auteur but not is_founding when cap hit), mirroring sync-entitlement. |
| BACKEND-SANITIZE-1 | supabase/functions/sanitize-input/index.ts | **Dead "server-side sanitizer".** Header claims it enforces sanitization to prevent client bypass, but it's invoked by NOTHING (repo-wide grep). No server-side sanitization choke point exists → reinforces COMP-1 (sanitization is client-only + inconsistent). Latent: profanity regex `\bn+i+g+` false-positives on `night_owl`/`nightmare`. | Tie to COMP-1: enforce in service layer + real server enforcement (DB trigger/CHECK or call this from write RPCs), or delete the dead fn. |
| BACKEND-PRIV-1 | mobile/supabase/migrations/20260609_security_definer_hardening.sql:187-240 · src/services/ProfileDataService.ts:235 | **Analytics RPC bypasses privacy.** `get_public_profile_analytics` (SECURITY DEFINER) only checks `auth.uid() IS NULL`, NOT `can_view_user_data(p_user_id)` → any authed user can directly fetch a PRIVATE user's aggregate analytics (total logs, avg rating, decades, autopsy averages, physical-media flag), re-exposing data the logs RLS protects. | Gate body on `can_view_user_data(p_user_id)`; add `STABLE`/`SET search_path=public`. |
| BACKEND-EMAIL-ENUM-1 | supabase/migrations/20260325_secure_username_lookup.sql:6-13 · src/stores/auth.ts:133 | **Email enumeration.** `get_email_by_username` (SECURITY DEFINER) returns any user's email by username; no explicit GRANT → PUBLIC execute (anon-callable), no rate-limit. Public usernames → anyone can harvest the whole userbase's emails (phishing/cred-stuffing). | Resolve username→email + sign-in in one server step that never returns email, or strict per-IP rate-limit, or email-only login. |
| BACKEND-NOTIF-DUP-1 | supabase/migrations/0002_premium_notifications.sql:41-44 · 0002_rls_hardening.sql:121-124 (+bodies in 20260613_*) | **Duplicate notifications on every follow/follow_request/endorse.** Two AFTER-INSERT triggers on `interactions` both insert a notification: `tr_notify_interaction`→`notify_on_interaction` (no counts) and `on_interaction_created`→`handle_interaction_notification` (also increments counts). Each migration only drops its own trigger name; neither drops the other → both fire. 2 notif rows (often 2 different `type` strings, e.g. `endorse` vs `reaction`) per event. Counts NOT double (only one fn increments). Comments unaffected (single trigger). | DROP one trigger — keep the count-bearing `on_interaction_created`/`handle_interaction_notification`; reconcile emitted `type` strings with notificationStore (ties to NOTIF-1/SVC-1). Add 1-interaction→1-notif regression. |
| COMP-1 | src/components/feed/DossierCritiquePanel.tsx:105 · app/dossier/[id].tsx:181 · ArticleReaderModal · ProfileTriptych:246 · ProgrammesSection:136 | Dossier comments + profile prefs use raw `supabase` writes in components, bypassing `DossierService`/`auth.setPreference`: online path skips `sanitizeInput` (offline path sanitizes — inconsistent) + Zod, duplicates offline-queue logic. | Route through service/store layer; put `sanitizeInput` in the service so both paths share it. |
| FOUND-1 ✅RESOLVED IN CODE | app/(modals)/membership.tsx:149-158 | 100-Founding-seat cap checked client-side (count → purchase) = TOCTOU race; two concurrent buyers can both pass. | **RESOLVED IN CODE** (no change needed): atomic row-locked `claim_founding_seat` RPC (migration 20260620) + `sync-entitlement` edge function calls it for founding; mobile `sync_entitlement` handler routes only through that function (never writes `is_founding`). Client count check is now a pre-flight UX guard. **Action: deploy migration + edge function.** |
| TYPES-3 | src/types/film.types.ts:9-66 · profile.types.ts:5-51 | Fragmented near-duplicate log/vault shapes; `autopsy:any`/`viewingHistory:any[]` defeat strict mode; pervasive snake/camel aliases. | Converge on `DomainLog`; derive others via Pick/Omit; type the `any`s; confine snake_case to row types. |
| TYPES-4 | src/types/react-native-purchases.d.ts:9-12 · src/lib/revenueCat.ts:44 | Stale stub types the installed SDK's default export as `unknown`; `Purchases:any` makes the entire payments layer uncompiled. | Delete stub; type handle as `typeof import('react-native-purchases').default`. |

| HOOK-1 ✅VERIFIED-REAL (worse) | src/hooks/useReportUser.ts:15 · src/components/home/PulseCardItem.tsx:77 · 20260429_trust_and_safety_engine.sql | **Pulse-card report is functionally inert.** It writes `user_reports` (separate table; Tribunal reads `reports` and never reads `user_reports`). The only effect is the `process_user_report` trigger deducting `trust_score -25` — but that feeds a **defeated shadowban** (see SHADOWBAN-1) and `trust_score` is read nowhere → reporting harassment via a pulse card accomplishes NOTHING. | Route PulseCardItem report through `reportStore.submitReport` → `submit_report` RPC → `reports` (reaches Tribunal), same as ReportSheet. Decide the fate of `user_reports`/trust_score (see SHADOWBAN-1). |
| BACKEND-SHADOWBAN-1 ✅VERIFIED | 20260429_trust_and_safety_engine.sql:36-87 | **Dormant, unreviewed auto-shadowban.** `user_reports` INSERT trigger deducts `trust_score -25` (no admin review, no notification; 4 unique reporters → 0 → "shadowban"). The shadowban logs-RLS ("visible if trust_score>0") is **defeated** because the later `can_view_user_data` privacy policy is OR-combined (public users stay visible at trust_score=0), AND 20260429 failed to drop the real permissive policy (wrong name). So today it's inert — but it's a latent brigading/censorship landmine: any "fix" to the privacy-OR interaction would suddenly activate mass shadowbans from accumulated deductions. trust_score read nowhere in client. | Decide intent: if shadowban is wanted, make it a RESTRICTIVE policy + admin-review threshold + appeal; if not, **remove the trust_score deduction trigger + the dead shadowban policy** and route reports to the Tribunal (HOOK-1). Don't leave a dormant censorship mechanism in the DB. |
| COMP-SPOILER-1 | src/components/log/LogForm.tsx:248 · src/components/feed/ReviewContent.tsx · src/components/log/LogReviewBody.tsx · app/log/[id].tsx:186 · feed.schema.ts | **"CONTAINS SPOILERS" toggle does nothing.** `is_spoiler` is collected + persisted + read into the domain model (`mappers.ts:198`) but **no UI ever branches on it** — review text marked as spoiler renders unguarded in the feed and on the log page. Decorative control → trust gap. | Consume `isSpoiler` on display: blur/tap-to-reveal in `LogReviewBody`; add `is_spoiler` to `feed.schema` + `ReviewContent` for a feed spoiler veil. |

## LOW
| ID | file:line | Issue | Fix |
|----|-----------|-------|-----|
| BACKEND-EMAIL-1 | supabase/functions/send-email/index.ts:22-38 | No auth gate — any caller can trigger welcome/digest emails to any userId (emails go to that user's own address, so spam + Resend cost, not exfiltration). In-memory rate-limit is per-edge-instance, not global. | Require service-role/JWT or shared secret (welcome/digest are signup-hook/cron triggered); DB-backed rate-limit. |
| BACKEND-EMAIL-2 | supabase/functions/send-email/index.ts:20 | `rateLimitCache` Map never evicts stale date-keyed entries → unbounded memory growth on long-lived worker. | Add TTL/size eviction (as tmdb-proxy does). |
| BACKEND-PUSH-1 | supabase/functions/notify-push/index.ts:26-31 | No caller verification on the DB-webhook target — forged `{record:{user_id,type}}` POST spams any user with canned push (bounded content). | Verify Supabase DB-webhook secret header / restrict to service-role. |
| BACKEND-PULSE-1 | supabase/functions/social-pulse/index.ts:38-46 | Service-role query bypasses `can_view_user_data` RLS → would leak private users' reviews into a public feed. Currently unwired/dead (mobile uses RLS-respecting direct query). | Delete dead fn, or use anon client / add `is_social_private=false` filter. |
| BACKEND-RL-1 | supabase/migrations/20260325_rate_limiting.sql:12-31 | `rate_limit_check` is SECURITY DEFINER with dynamic EXECUTE over unqualified identifiers + no `SET search_path` → theoretical rate-limit bypass via search_path shadowing. Exploitability LOW (Supabase authenticated can't create shadowing tables by default); inconsistent with codebase convention. | Add `SET search_path = public`; schema-qualify table refs. |
| BACKEND-TIP-1 (if shipped) | supabase/migrations/20260326_rls_hardening_round19.sql:82-124,14-68 | **Forgeable tips / free seat booking.** `process_secure_tip` records a tip + bumps `tip_total` with NO payment verification, client-callable (author comment: should be service_role) → fake earnings inflation. `book_showtime_seat` books seats free, no auth/payment/buyer tracking. Projectionist/venue features (likely outside core mobile scope). Also: `protect_video_review_metrics` repeats the PROFILE-FREEZE-1 revert bug (views/tip_total frozen). | If these features ship: payment-confirmed service-role tip path; ownership+payment on seat booking. Fix the protect-trigger pattern (see PROFILE-FREEZE-1). |
| BACKEND-LOUNGE-1 | supabase/migrations/20260401_the_lounge.sql:176-185 | "Invite code lookup" SELECT policy gates on `invite_code IS NOT NULL` (not matching a provided code) → any archivist+ can `SELECT * FROM lounges` and enumerate ALL private lounges' metadata (name/description/creator/member_count) without membership. Messages stay protected; undermines "private" promise. | Drop the broad policy; lookup via SECURITY DEFINER `find_lounge_by_invite(p_code)` returning only the matching row. |
| BACKEND-TMDB-1 | supabase/functions/tmdb-proxy/index.ts | Unauthenticated, no rate-limit → open proxy can burn the TMDB key's quota (key stays hidden; data is public). | Add anon-key/JWT check or per-IP throttle. |
| TYPES-2 | src/types/mutations.ts:81-82 | `submit_report` inlines moderation enums instead of importing `ReportableContentType`/`ReportReason`; drift risk. | Import the shared z.enums. |
| SCHEMA-4b | src/schemas/profile.ts:20 · src/hooks/useEditProfile.ts:15-20 | Exported `EditProfileSchema` is dead (its documented consumer redefines it inline, dropping the `max(10)` links cap). | Import the shared schema in useEditProfile. |
| HOOK-2 | src/providers/AccessibilityProvider.ts:20-41 | Monkey-patches RN `Text.render`/`TextInput.render` (upgrade-fragile, has fallback). | Pin RN version in a comment + smoke test. |
| SCHEMA-1 | src/schemas/profile.ts:17,21-24 | `EditProfileSchema` username regex looser than `validateUsername` (accepts `_x`/`x_`/`x__y`); doc says "3-20" but code is max(30). | Use `superRefine(validateUsername)`; fix comment. |
| SCHEMA-2 | src/schemas/settings.ts:11-12 · user.ts:14-15 | Privacy enums not enforced in persisted `UserPreferencesSchema` (bare `z.string()`). | Share the `z.enum` across both. |
| SCHEMA-3 | src/schemas/feed.schema.ts:40,75,109 · user.ts:22-23 | `z.any()` for JSONB leaks `any`. | Use `z.unknown()` or concrete shapes. |
| SCHEMA-4 | src/schemas/film.schema.ts:6-63 · profile.ts/profile.schema.ts | `DomainLogSchema` hand-mirrors the interface (drift); confusing two-file profile schema split. | Derive type from schema via `z.infer`; consolidate/rename profile schema files. |
| CONST-1 | src/theme/theme.ts:23-27 | "Derived" sepia rgba uses a different hue (196,150,26) than base `sepia` (#B8891A). | Derive from base channels or rename. |
| CONST-2 | src/constants/index.ts:7-9 | Barrel omits `formats`/`textScaling` despite "all constants" claim. | Add exports or soften doc. |
| CONST-3 | src/constants/membership.ts:28-30,56-57 | Hardcoded USD prices risk drift from StoreKit localized pricing. | Source displayed price from RC offering's `priceString`. |
| LIB-3 | src/lib/defensiveParse.ts:8-12 | Header says "PROD returns raw data"; code throws. | Update header. |
| LIB-4 | ARCHITECTURE.md · (no file) | Documents non-existent `src/lib/featureFlags.ts`. | Remove doc section or restore impl. |
| LIB-5 | src/lib/schemas.ts:15 | `TmdbIdSchema` duplicates id coercion. | Share one coercer. |
| OFFQ-2 | src/utils/offlineQueue.ts:42-48 | `_queueUserId` write-only dead state; misleading "user-scoped" comment. | Remove or wire up; fix comment. |
| UTIL-1 | src/utils/mutationExecutor.ts:734-735 | Fixed 100ms delay before every mutation → ~10s for a full queue. | Yield every N, or smaller delay. |
| UTIL-2 | src/utils/withTimeout.ts:39-46 | External AbortError reported as "timed out". | Distinguish timeout signal from external abort. |
| UTIL-3 | src/utils/timeAgo.ts:24 | Drops year for dates >30 days (ambiguous old dates). | Include year past ~1yr. |
| UTIL-4 | src/utils/AppError.ts:162 | Generic "database error saving new user" mapped to "Username taken". | Only map on 23505 username unique-violation. |
| SVC-2 | src/services/ProfileDataService.ts:413,457 · FilmService.ts:72 · ProfileWriteService.ts:203,228 | Inconsistent cursor sanitization: raw `cursorDate` interpolated into `.or()` (RLS-contained but a known injection surface; `FeedService.parseCursor` is the fix). | Route all cursors through `parseCursor`-style ISO/UUID shape validation. |
| SVC-3 | src/services/NewsService.ts:50-55 | Local `decodeEntities` weaker than `utils/html.ts`. | Import shared decoder. |
| STORE-1 | src/stores/auth.ts:249-256 | Logout skips MMKV purge on web → stale auth/cross-user data on shared browser (MEDIUM if web ships). | Clear MMKV on web too, or drop web support. |
| STORE-2 | src/stores/auth.ts:57-125 | `restoreSession` leaves optimistic `isAuthenticated:true` if no session found (relies on a separate listener to correct). | Clear auth state in the no-session branch. |

## Needs server verification (not client defects)
- `tier.ts` client entitlement gating — ensure paid features are server-enforced (RLS/edge), not only client tier checks.
- Cross-user `notifications` inserts (StackService + mutationExecutor) — confirm RLS constrains them (spam vector).
- FeedService community-feed direct fallback can't filter blocks server-side — deploy `get_community_feed_auth_cursor`.
